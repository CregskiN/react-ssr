import React from 'react';

const Home = () => {
    
    return (
        <div>
            <div>welcome to home</div>
            <button onClick={() => {console.log('on click')}}>click</button>
        </div>
    )
};

export default Home;